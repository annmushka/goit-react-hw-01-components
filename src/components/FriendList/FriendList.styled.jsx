import styled from '@emotion/styled';

export const Friends=styled.ul` margin: 100px 0;
display: flex;
flex-direction: row;
flex-wrap: wrap;
gap: 20px;
justify-content: center;
list-style-type: none;
`;