import styled from '@emotion/styled';

export const Friends=styled.ul` margin: 50px 0;
display: flex;
flex-direction: row;
flex-wrap: wrap;
gap: 15px;
justify-content: center;
list-style-type: none;
`;